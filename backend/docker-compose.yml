version: '3.8'

services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: jetlag_coach
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  backend:
    build: .
    environment:
      DATABASE_URL: postgresql://user:password@db:5432/jetlag_coach
      REDIS_URL: redis://redis:6379
      JWT_SECRET: your-secret-key-change-in-production
      FRONTEND_BASE_URL: http://localhost:3000
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis
    volumes:
      - .:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  celery:
    build: .
    environment:
      DATABASE_URL: postgresql://user:password@db:5432/jetlag_coach
      REDIS_URL: redis://redis:6379
      JWT_SECRET: your-secret-key-change-in-production
    depends_on:
      - db
      - redis
    volumes:
      - .:/app
    command: celery -A app.services.scheduler worker --loglevel=info

volumes:
  postgres_data:
